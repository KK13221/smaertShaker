<?xml version="1.0" encoding="utf-8"?>
<!--
    SmartShake font family declarations.

    SETUP INSTRUCTIONS
    ──────────────────
    1. Download the following font TTF files and drop them into this same
       res/font/ directory (filenames must match exactly):

       Poppins (https://fonts.google.com/specimen/Poppins)
         poppins_light.ttf          → Poppins-Light.ttf        (weight 300)
         poppins_regular.ttf        → Poppins-Regular.ttf      (weight 400)
         poppins_medium.ttf         → Poppins-Medium.ttf       (weight 500)
         poppins_semibold.ttf       → Poppins-SemiBold.ttf     (weight 600)

       Inter (https://fonts.google.com/specimen/Inter)
         inter_regular.ttf          → Inter-Regular.ttf        (weight 400)
         inter_bold.ttf             → Inter-Bold.ttf           (weight 700)

       Reddit Sans (https://fonts.google.com/specimen/Reddit+Sans)
         reddit_sans_medium.ttf     → RedditSans-Medium.ttf    (weight 500)

    2. Rename each downloaded file to the snake_case name shown in the left
       column above (Android requires lowercase a-z, 0-9 and underscore only).

    3. Sync / rebuild — Android Studio will auto-generate R.font.* references.

    NOTE: These font family XML files are provided for reference only.
    The layouts reference each font directly via android:fontFamily="@font/..."
    so these XML font-family files are NOT required for the layouts to compile —
    only the individual .ttf files in res/font/ are needed.
-->

<!-- Poppins font family -->
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <font
        android:font="@font/poppins_light"
        android:fontStyle="normal"
        android:fontWeight="300"
        app:font="@font/poppins_light"
        app:fontStyle="normal"
        app:fontWeight="300" />

    <font
        android:font="@font/poppins_regular"
        android:fontStyle="normal"
        android:fontWeight="400"
        app:font="@font/poppins_regular"
        app:fontStyle="normal"
        app:fontWeight="400" />

    <font
        android:font="@font/poppins_medium"
        android:fontStyle="normal"
        android:fontWeight="500"
        app:font="@font/poppins_medium"
        app:fontStyle="normal"
        app:fontWeight="500" />

    <font
        android:font="@font/poppins_semibold"
        android:fontStyle="normal"
        android:fontWeight="600"
        app:font="@font/poppins_semibold"
        app:fontStyle="normal"
        app:fontWeight="600" />

</font-family>
